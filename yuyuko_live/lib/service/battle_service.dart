import 'package:logging/logging.dart';
import 'package:yuyuko_live/service/file_service.dart';

class BattleService extends FileService {
  final _logger = Logger('BattleService');
  BattleService({required path}) : super(path: path);

  // Method to covert the json string to an object
}